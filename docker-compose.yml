version: "3.9"

services:
  load-balancer:
    build:
      context: .
      dockerfile: LoadBalancer/Dockerfile
    ports:
      - 9000:80
  api-search:
    build:
      context: .
      dockerfile: "ApiSearch/Dockerfile"
    volumes:
      - "C:\\University Work\\SearchEngine-Baseline\\EnronMini:/data"
    deploy:
      replicas: 5
    depends_on:
      - load-balancer
  web-ui:
    build:
      context: .
      dockerfile: "WebSearch/Dockerfile"
    ports:
      - 9001:80
